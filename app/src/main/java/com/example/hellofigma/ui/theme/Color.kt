package com.example.hellofigma.ui.theme

import androidx.compose.ui.graphics.Color

// Primary Colors
val Primary = Color(0xFF4741AF)
val LinkBlue = Color(0xFF2A9CEE)
val FacebookBlue = Color(0xFF3B5998)

// Background Colors
val LightGray = Color(0xFFF5F5F5)
val White = Color(0xFFFFFFFF)

// Text / Neutral Colors
val GrayText = Color.Gray
val BlackText = Color.Black

// Alerts / Errors
val ErrorRed = Color.Red
val TrueRed = Color(0xFFFF3B30)

// Gradient & Accents
val PurpleLight = Color(0xFF7B61FF)
val GradientEnd = Color(0xFF7B74D6)

// Overlays & Transparency
val SemiTransparentBlack = Color.Black.copy(alpha = 0.5f)
val OverlayDark = Color.Black.copy(alpha = 0.7f)

// UI Specific Backgrounds
val BottomNavBackground = Color(0xFFFBFBFB)
val SearchBarBackground = Color(0xFFE6E6E6)
val CardBackground = Color.White

// Text & Icons
val TextSecondary = Color(0xFF666666)
val DeleteRed = Color(0xFFD32F2F)

// ──────────────────────────
// NEW COLORS ADDED (for disease pages — add at the end)
val SecondaryText = Color(0xFF7C7B7B)          // Secondary text (hints, subtitles)
val DescriptionText = Color(0xFF555555)        // Product descriptions
val ImagePlaceholder = Color(0xFFD3D3D3)       // Placeholder behind product images
// ──────────────────────────